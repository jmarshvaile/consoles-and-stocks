library(quantmod)
?getSymbols

# Gamestop
getSymbols(Symbols = 'GME')

#
# Below are the top 10 companies by total exposure
#

# Microsoft Corporation -- total exposure
getSymbols(Symbols = 'MSFT')

# Sony Corporation -- total exposure
getSymbols(Symbols = 'SNEJF')

# Apple Inc. -- total exposure
getSymbols(Symbols = 'AAPL')

# Nintendo Co., Ltd. -- total exposure
getSymbols(Symbols = 'NTDOF')

# Electronic Arts Inc. -- total exposure
getSymbols(Symbols = 'EA')

# Activision Blizzard, Inc. -- total exposure
getSymbols(Symbols = 'ATVI')

# Take-Two Interactive Software -- total exposure
getSymbols(Symbols = 'TTWO')

# Funko, Inc. -- total exposure
getSymbols(Symbols = 'FNKO')

# Turtle Beach Corporation -- total exposure
getSymbols(Symbols = 'HEAR')

# LSC Communications, Inc. -- total exposure
getSymbols(Symbols = 'LKSD')

# Guillemot Corporation S.A. -- financial exposure
getSymbols(Symbols = 'GUI.PA')

# Pluralsight, Inc. -- financial exposure
getSymbols(Symbols = 'PS')

# Hasbro, Inc. -- financial exposure
getSymbols(Symbols = 'HAS')

# Carmila S.A. -- media exposure
getSymbols(Symbols = 'CARM.PA')
